# --- Connection Stability (Highly Compatible) ---
# This is the updated, safe way to increase Bluetooth signal strength
# without causing Kconfig compiler aborts
#CONFIG_BT_CTLR_TX_PWR_DYNAMIC_CONTROL=y
#CONFIG_BT_CTLR_TX_PWR_MAX_PLUS_8=y

# --- Battery reporting settings ---
# Only define the proxy (safe for both halves)
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_PROXY=y

# --- Optional features (Uncomment if you want them) ---
# CONFIG_ZMK_SLEEP=y
# CONFIG_ZMK_DISPLAY=y
